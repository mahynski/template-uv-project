[project]
name = "template-uv-project"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pytest-cov>=6",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
